How to build (requires gcc)

Required libraries: `gflags`

To install that from Ubuntu / Debian / Raspbian:

```bash
sudo apt install libgflags-dev
```

make

Building for arm-linux (requires arm-linux-gcc toolchain)

make TARGET=arm-linux

Binaries will be in src/$(uname -m)/bin

